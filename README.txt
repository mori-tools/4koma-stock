4コマSTOCK v1.5 COMPLETE
========================

今回の変更
- おはようSTOCKで実際に動作している画像コピー処理を移植
- 各カードに「画像コピー」を追加
- PNG以外はCanvasでPNG化してClipboardへコピー
- 画像なしネタでは「画像がありません」と表示
- 「投稿文コピー」「画像コピー」「ステータス変更」「編集」を2×2配置
- 既存のSupabase同期、認証、新アイコンは維持
- app.js?v=1.5 / Service Worker v1.5 でキャッシュ更新

GitHubの 4koma-stock リポジトリへ、このフォルダの中身をすべて上書きしてください。
